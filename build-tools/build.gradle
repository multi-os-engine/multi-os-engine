plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        buildTools {
            id = "org.moe.buildtools"
            implementationClass = "org.moe.prebuilts.BuildTools"
        }
    }
}
